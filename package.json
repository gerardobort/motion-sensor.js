{
  "name": "motion-sensor.js",
  "description": "Uses HTML5 getUserMedia() to detect multi-segment motion.",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "4.x"
  }
}
